apiVersion: apps/v1
kind: Deployment
metadata:
  name: tekton-app-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tekton-app-frontend
  template:
    metadata:
      labels:
        app: tekton-app-frontend
    spec:
      containers:
        - name: tekton-app-frontend
          image: webapp:1
          # for local development against minikube registry
          #imagePullPolicy: Never
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-app-service
  labels:
    app: frontend-app-service
spec:
  type: NodePort
  ports:
    # the port that this service should serve on
    - port: 80
      targetPort: 8080
  # label keys and values that must match in order to receive traffic for this service
  selector:
    app: tekton-app-frontend
